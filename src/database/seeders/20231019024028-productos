'use strict';
const productsJSON = require('../../data/items.json')

const products = productsJSON.map(({  name, model, description, price, discount, brandId, imageId, statusId, categoryId, }) => {
  return {
    name,
    model,
    description,
    price,
    discount,
    brandId,
    imageId,
    statusId,
    categoryId,
    createdAt: new Date(),
    updatedAt: new Date()
  }
})
/** @type {import('sequelize-cli').Migration} */
module.exports = {
  async up(queryInterface, Sequelize) {


    await queryInterface.bulkInsert('Product', products, {});//primer valor es nombre de tabla, segundo valor es con los datos q se llena la tabla en este caso la const categorie

  },

  async down(queryInterface, Sequelize) {
    /**
     * Add commands to revert seed here.
     *
     * Example:
     * await queryInterface.bulkDelete('People', null, {});
     */
  }
};
